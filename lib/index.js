'use strict';

const de = require('./de');
const deep = require('./deep')
const safe = require('./safe');
const type = require('./type');

module.exports = {
  de,
  deep,
  type,
  safe
}